# ⚡️ Императивный менеджер модальных окон Ant Design

Эта легковесная утилита предоставляет **императивный API** для управления модальными окнами `antd`.

Она решает классическую проблему: "Как открыть модальное окно из thunk, сервиса или глубоко вложенного компонента, не прибегая к **prop drilling** или захламлению глобального стейта (Redux/Context)?"

Менеджер использует простую **pub/sub систему** на хуке `createUseExternalEvents`, позволяя вызывать модальные окна из *любой* точки вашего React-приложения.

## 💡 Пример использования

### Вызов модального окна (из любого файла)

Импортируйте ваш инстанс `modals` и вызывайте его методы напрямую из любого компонента или сервиса.

```jsx
import { Button } from 'antd';
import { modals } from './Modals'; // Ваш инстанс менеджера
import { MyCustomForm } from './MyCustomForm';

export const MyComponent = () => {

  // Вызов модального окна с кастомным компонентом
  const handleOpenForm = () => {
    modals.openModal({
      modalId: 'edit-user-form',
      component: <MyCustomForm userId={123} />,
      modalProps: {
        title: 'Редактировать пользователя',
        footer: null, // Скрываем футер, если у формы свои кнопки
      }
    });
  };

  // Вызов стандартного модального окна подтверждения
  const handleShowConfirm = () => {
    modals.openConfirmModal({
      modalId: 'confirm-delete',
      modalProps: {
        title: 'Подтвердите действие',
        content: 'Вы уверены, что хотите удалить этот элемент?',
        onOk: () => {
          console.log('Элемент удален');
        }
      }
    });
  };

  return (
    <div>
      <Button onClick={handleOpenForm}>Редактировать</Button>
      <Button danger onClick={handleShowConfirm}>Удалить</Button>
    </div>
  );
};
